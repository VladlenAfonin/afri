\documentclass{standalone}

\usepackage{fontspec}
\setmainfont{Times New Roman}

\usepackage[main = russian, english]{babel}
\usepackage{unicode-math}
% \setmathfont{Cambria Math}
\usepackage{pgfplots}

\begin{document}
\begin{tikzpicture}
    \begin{axis}[
        title = {Проверяющий},
        xlabel = {Степень начального многочлена},
        ylabel = {Время работы, мс},
        xticklabel = {$2^{\pgfmathprintnumber{\tick}}$},
        grid = major,
        legend entries = {aFRI,FRI},
        legend pos = north west
    ]
        \addplot [
            color = red,
            mark = *
        ] table [
            y expr = \thisrow{avg} / 1000000,
            x = benchmark,
            y = avg,
            col sep = comma
        ] {afri-verifier.csv};
        \addplot [
            color = blue,
            mark = square
        ] table [
            y expr = \thisrow{avg} / 1000000,
            x = benchmark,
            y = avg,
            col sep = comma
        ] {fri-verifier.csv};
    \end{axis}
\end{tikzpicture}
\end{document}
