\documentclass{standalone}

\usepackage{fontspec}
\setmainfont{Times New Roman}

\usepackage[main = russian, english]{babel}
\usepackage{unicode-math}
% \setmathfont{Cambria Math}
\usepackage{pgfplots}

\begin{document}
\begin{tikzpicture}
    \begin{axis}[
        title = {Доказывающий},
        xlabel = {Длина вычислительного следа},
        ylabel = {Время работы, мс},
        xticklabel = {$2^{\pgfmathprintnumber{\tick}}$},
        ymode = log,
        grid = major,
        legend entries = {aSTARK,STARK},
        legend pos = north west
    ]
        \addplot [
            color = red,
            mark = *
        ] table [
            y expr = \thisrow{avg} / 1000000,
            x = benchmark,
            y = avg,
            col sep = comma
        ] {astark-prover.csv};
        \addplot [
            color = blue,
            mark = square
        ] table [
            y expr = \thisrow{avg} / 1000000,
            x = benchmark,
            y = avg,
            col sep = comma
        ] {stark-prover.csv};
    \end{axis}
\end{tikzpicture}
\end{document}
